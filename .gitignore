# Dépendances
node_modules/

# Build (généré par GitHub Actions)
build/
build.zip

# Secrets — NE JAMAIS committer
.env
.env.production
.env.local
.env.*.local

# Fichiers système
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Claude Desktop
claude-path.json

# Cache
.cache/

# Backups Firestore (générés par le workflow, publiés en artifacts — jamais committés)
backup/
# Clé de service Firebase éventuellement écrite localement
firebase-service-account*.json

# Anciennes copies locales
proddddd/

# Export Firestore + dump SQL de migration (données réelles + jetons WP — jamais committé)
migration/export/
migration/dump/

# Paramètres serveur (clés API — ne jamais committer)
data/
.claude/MEMORY.md
.claude/CLAUDE.md

# Config de lancement du dev server (locale) — la règle 3 interdit de démarrer
# le serveur depuis l'agent : ce fichier ne doit pas être suivi.
.claude/launch.json
